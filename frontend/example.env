# Proton Prediction Market - Environment Configuration
# Copy this file to .env and update with your values

# =============================================================================
# APPLICATION CONFIGURATION
# =============================================================================

# Application name (displayed in browser tab and header)
REACT_APP_NAME=Proton Prediction Market

# =============================================================================
# TESTNET CONFIGURATION (for development and testing)
# =============================================================================

# Proton testnet RPC endpoint
REACT_APP_PROTON_ENDPOINT=https://testnet.protonchain.com

# Your deployed smart contract account name on testnet
# Replace with your actual contract account after deployment
REACT_APP_CONTRACT_NAME=your-contract-account

# Proton testnet chain ID
REACT_APP_CHAIN_ID=71ee83bcf52142d61019d95f9cc5427ba6a0d7ff8accd9e2088ae2abeaf3d3dd

# Token contract for XPR transfers (default: eosio.token)
REACT_APP_TOKEN_CONTRACT=eosio.token

# =============================================================================
# MAINNET CONFIGURATION (for production deployment)
# =============================================================================
# Uncomment and use these values for mainnet deployment:

# REACT_APP_PROTON_ENDPOINT=https://proton.greymass.com
# REACT_APP_CONTRACT_NAME=your-mainnet-contract
# REACT_APP_CHAIN_ID=384da888112027f0321850a169f737c33e53b388aad48b5adace4bab97f437e0
# REACT_APP_TOKEN_CONTRACT=eosio.token

# =============================================================================
# IMPORTANT NOTES
# =============================================================================
# 
# 1. Environment variables are compiled into the JavaScript bundle during build
#    - Changes to .env require rebuilding: npm run build
#    - Variables are NOT read at runtime
#
# 2. All React environment variables must start with REACT_APP_
#    - This is a Create React App requirement
#    - Variables without this prefix will be ignored
#
# 3. Never commit .env files to version control
#    - .env is already in .gitignore
#    - Use this example.env as a template
#
# 4. For production deployment:
#    - Update .env with production values
#    - Run: npm run build
#    - Deploy the build/ directory to your hosting provider
#
# 5. Contract account name:
#    - Must match your deployed smart contract account on Proton blockchain
#    - Create account at: https://testnet.protonchain.com (testnet)
#    - Create account at: https://proton.org (mainnet)
#
# 6. Session persistence:
#    - Wallet sessions are automatically persisted across page refreshes
#    - Sessions are stored securely using Proton's built-in session management
#    - Users stay logged in until they explicitly disconnect
#    - Session data is stored in browser storage under the app name
#
# =============================================================================
# SETUP INSTRUCTIONS
# =============================================================================
#
# 1. Copy this file:
#    cp example.env .env
#
# 2. Edit .env and replace 'your-contract-account' with your actual contract name
#
# 3. Choose testnet or mainnet configuration
#
# 4. Install dependencies:
#    npm install
#
# 5. Start development server:
#    npm start
#
# 6. For production build:
#    npm run build
#
# =============================================================================
